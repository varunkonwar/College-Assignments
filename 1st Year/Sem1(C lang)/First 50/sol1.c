// calculate the sum of numbers
#include <stdio.h>
int main (){
    
    float a, b;
    printf ("Input the two numbers here: ");
    scanf ("%f%f", &a, &b);
    printf ("The sum is: %f", a + b);

    return 0;
}